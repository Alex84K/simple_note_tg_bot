services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # БД и файлы живут на хосте — переживают пересборку контейнера
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
